[build]
builder = "nixpacks"

[deploy]
startCommand = "streamlit run scripts/japan-dashboard.py --server.port $PORT --server.address 0.0.0.0 --server.headless true --server.enableCORS false --server.enableXsrfProtection false"
restartPolicyType = "on_failure"
