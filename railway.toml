[build]
builder = "nixpacks"

[deploy]
startCommand = "streamlit run scripts/japan-dashboard.py --server.port $PORT --server.headless true"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
